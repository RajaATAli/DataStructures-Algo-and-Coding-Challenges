/*

You have an array of integers, for example [1, 2, 3, 4, 5]. Write a function that rotates this array to the right by k steps, where k is a non-negative integer. For instance, if k = 2, the array should become [4, 5, 1, 2, 3].
*/


#include <iostream>

int main(){

	// Could use dynamic array -> std::vector (ArrayList in Java) -> for this problem but we're aiming for an in-place solution
	// Manipulating elements within the vector can be inefficient (especially front and middle)


	return 0;
}

